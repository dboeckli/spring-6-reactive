spring:
  application:
    name: spring-6-reactive
  datasource:
    url: jdbc:h2:mem:testdb
    driverClassName: org.h2.Driver
    username: sa
    password: password
  # TODO: console does not show up: http://localhost:8080/h2-console
  h2:
    console:
      enabled: true
      path: /h2-console
      settings.trace: false
      settings.web-allow-others: false

logging:
  level:
    org.springframework.r2dbc: DEBUG
